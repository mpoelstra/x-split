import { AppEnvironment } from '../app/core/env/app-environment';

export const environment: AppEnvironment = {
  production: false,
  mode: 'stub',
  authBypass: true,
  appName: 'X-Split',
  supabaseUrl: '',
  supabaseAnonKey: ''
};
